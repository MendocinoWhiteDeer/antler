#version 460

layout(std430, binding=0) readonly buffer vecA
{
	float a[];
};

layout(std430, binding=1) readonly buffer vecB
{
	float b[];
};

layout(std430, binding=2) writeonly buffer vecC
{
	float c[];
};

//layout (local_size_x = 256) in;

void main()
{
	const uint i = gl_GlobalInvocationID.x;
	c[i] = a[i] + b[i];
}